# AI 视频生产自动化工作流方案（草稿 v1.0）

> 路线：纯免费 / 零成本优先 + 多模态参考工作流（H3 Ref2VA）+ WorkBuddy 自动排单
> 状态：草稿，需与 Lionel 一起迭代完善。文中 `[待你补充]` 处需要你填。
> 配套调研：见同目录 `AI视频生产工作流与变现调研报告.md`

---

## 0. 这份文档怎么用

- 这是**第一版框架**，目标是把"你脑子里的工作流"落成文，再逐步把它变成 WorkBuddy 能自动执行的工具。
- 凡标 `[待你补充]` 的，是我不确定的地方，你填了我就补进下一版 / 或据此写工具。
- 技术前提已实测成立（见 §6.1），所以方案是落得了地的，不是蓝图。

---

## 1. 目标与原则

1. **纯免费 / 零成本优先**：素材能用免费平台就用免费平台；本机台式机本地模型是"零边际成本"的底盘；公司工具（image2 / 大香蕉）只作兜底。
2. **多模态参考工作流**：最终生成走 H3 的 Ref2VA——把"想到的素材"（场景图 / 角色图 / 声音 / 参考视频）直接作为参考喂进去，而不是纯文本生视频。素材越齐，成片越可控。
3. **分工明确**：
   - 你（导演）= 创意设计、拍板、看结果
   - WorkBuddy（我）= 整理素材、提示词工程、把素材包编译成 ComfyUI 工作流、经 API 排单、监控、回收结果
   - 台式机 ComfyUI = 执行生成（单一事实源，笔记本已不再装 ComfyUI）
4. **资产沉淀**：每一集的素材、提示词、对话、导演台文档、生成结果，都结构化存到笔记本工作区，可被 WorkBuddy 直接读取复用。

---

## 2. 总体数据流

```
┌─────────────┐   素材包/提示词/对话    ┌──────────────┐  工作流JSON+素材    ┌──────────────────┐
│  笔记本      │ ───────────────────▶  │  WorkBuddy   │ ───(API上传+排单)──▶ │  台式机 ComfyUI  │
│ (你设计/存稿) │                        │ (整理/编译/  │                    │  (H3 生成视频)    │
│ 导演台文档   │ ◀─── 结果回传/整理成稿 ─│  监控/排单)  │ ◀──(history拉结果)── │  RTX4060Ti 16G   │
└─────────────┘                        └──────────────┘                    └──────────────────┘
       ▲                                                                        │
       └──────────────── 你审阅 → 拍板通过 / 打回重排 ◀────────────────────────┘
```

核心闭环：**你只管开头设计 + 最后拍板看结果；中间"素材→工作流→排单→监控→回收"全部由 WorkBuddy 自动化。**

---

## 3. 素材生产矩阵（纯免费路线）

> 设计哲学：**多模态参考工作流 = 素材即控制**。所以"想到就能找到"的素材（场景/角色/声音/参考视频）是生产的地基，H3 负责把它们"揉"成视频。

| 素材类型 | 在 H3 多模态里的角色 | 免费平台渠道 | 台式机本地（零成本） | 公司兜底 `[待你补充]` |
|---|---|---|---|---|
| **场景图** | 参考图（Ref2VA，最多 9 张） | 即梦 / 海艺(4K免费) / Vidu / 可灵(每日免费) | 本地 SDXL / Flux 生图 | image2 / 大香蕉 |
| **角色图** | 角色锁定核心（Ref2VA，固定人设） | 同上 + 即梦"角色一致性" | 本地 Flux + 角色 LoRA | image2 / 大香蕉 |
| **声音素材**（对白/音效） | 音频参考（最多 3 段，口型/声线） | 剪映 TTS / Fish Audio 免费 / ElevenLabs 免费额度 | 本地 GPT-SoVITS / CosyVoice（声音克隆） | 大香蕉 `[待补]` |
| **参考视频**（运动/风格） | 视频参考（最多 3 段） | 可灵 / 即梦 免费片段 | 本地 H3 先出草稿再当参考 | image2 / 大香蕉 |
| **首尾帧**（可选） | FL2VA 首末帧控制 | 即梦 / 海艺 生图 | 本地 Flux | image2 |
| **提示词 / 分镜 / 对话** | 文本输入 | WorkBuddy / 豆包 / DeepSeek 免费 | — | — |

**H3 Ref2VA 规格（已确认，来自之前调研）**：文本 + 图像(≤9) + 视频(≤3) + 音频(≤3) → 视频 + 32kHz 立体声；稳定支持 11 种对白语言；本地封顶 768p（短边），2K 走托管 API。

---

## 4. 多模态参考工作流（已实测台式机支持 ✅）

实测台式机 ComfyUI（v0.30.1）**已装好 H3 参考生成节点**，这是整个方案的技术地基：

| 节点名 | 用途 |
|---|---|
| `MiniMaxH3ReferenceToVideo` | **Ref2VA 核心**：图+视频+音频参考 → 视频 |
| `MiniMaxH3ImageToVideo` | FL2VA：图生视频 / 首尾帧 |
| `EmptyMiniMaxH3LatentAV` | 潜变量初始化（含音频轨道） |
| `MiniMaxH3SigmaShift` | 生成参数（sigma shift） |
| `LTXVReferenceAudio` / `ReferenceTimbreAudio` | 音频参考（口型 / 声线） |

> 共 827 个节点类型可用，除 H3 外还有 FluxKontext 多参考、字节/千问/ Vidu / Wan / Kling 等参考节点可作备选。

**一集的标准生成单元怎么组织参考素材**（建议）：
- 角色图 1–N 张（固定人设，喂 Ref2VA 角色锁定）
- 场景图 1–M 张（每个场景一张氛围参考）
- 声音 1–3 段（角色对白音色 / 关键音效）
- 参考视频 0–3 段（想要的运动风格 / 镜头语言）
- 文本：每镜头提示词 + 对白

---

## 5. 角色与分工边界（写清楚，避免以后扯皮）

| 角色 | 负责 | 不负责 |
|---|---|---|
| **你（导演）** | 创意设定、分镜/人设拍板、最终审阅与通过/打回 | 不手敲工作流、不手动传参、不盯进度 |
| **WorkBuddy（我）** | 素材归档、提示词工程、把"素材包"编译成 ComfyUI 工作流 JSON、经 API 上传素材+排单、WS 监控、回收结果并整理成稿 | 不做创意决策、不擅自执行（需你拍板） |
| **台式机 ComfyUI** | 按工作流执行生成、产出视频 | 不主动设计、不判好坏 |

**关键约定**：WorkBuddy 排好的任务**先标"待拍板"**，你确认后才真正提交执行；你也可说"这批全跑"放行整批。

---

## 6. 自动化方案：WorkBuddy ↔ 台式机 ComfyUI 对接（核心）

### 6.1 我能拿到什么？（2026-08-08 实测结论）

从笔记本上运行的 WorkBuddy，经 TailScale 访问 `http://100.67.139.74:8188`，**全部实测可达**：

| 能力 | API | 实测结果 |
|---|---|---|
| 系统状态 | `GET /system_stats` | ✅ v0.30.1，32GB 内存，当前有 1 个任务在跑 |
| 队列 | `GET /queue` | ✅ running / pending 实时可读 |
| 历史 + 结果 | `GET /history` | ✅ 含每次生成的输出（= 结果日志） |
| 节点清单 | `GET /object_info` | ✅ 827 个节点类型，H3 参考节点齐全 |
| 提交任务 | `POST /prompt` | ✅ 提交工作流 JSON |
| 上传素材 | `POST /upload/image` | ✅ 上传参考图/视频 |
| 实时进度/节点日志 | `WS /ws` | ✅ WebSocket 监控 |

**两个必须说清的限制**：
1. ComfyUI **没有"GET 当前画布工作流"的 API**——画布状态只在前端。所以我们的做法是：**工作流以 JSON 文件存在笔记本工作区**，由 WorkBuddy 生成/修改并提交，这比"读画布"更可控、更适合自动化。
2. 控制台文本日志不在 REST 里，但节点执行状态/报错在 `/history` 和 `/ws` 里能拿到，够用。
3. 前提：台式机开机 + ComfyUI 运行 + TailScale 连通（链路已验证稳定）。

### 6.2 自动排单闭环（设想流程）

```
1. 你设计
   └─ 在笔记本工作区建"一集素材包"（图/音频/视频/提示词/对话/导演台文档 brief.md）
2. WorkBuddy 读取素材包
   └─ 按模板生成 ComfyUI 工作流 JSON（填入 Ref2VA 参考、提示词、参数：步数/时长/分辨率/种子）
3. WorkBuddy 经 API
   ├─ upload 素材 → 组装 prompt → 标记「待拍板」
4. 等你拍板
   └─ 我列出待执行清单，你确认后才真正提交（或说"全跑"）
5. 执行中
   └─ WS 监控进度；完成从 /history 拉结果 → 下载到笔记本 output/
6. 结果回传你审阅
   └─ 通过 / 打回（打回则调参重排）
```

### 6.3 目录约定（笔记本工作区）

```
F:\WorkBuddy\AI_Video\
├─ episodes/
│  └─ ep01_<标题>/
│     ├─ brief.md          # 导演台设计文档（设定/分镜表/镜头表/对白）
│     ├─ prompts/
│     │  └─ scene_01.txt … # 每镜头提示词
│     ├─ assets/
│     │  ├─ scenes/        # 场景参考图
│     │  ├─ characters/    # 角色参考图
│     │  ├─ audio/         # 声音素材
│     │  └─ video_ref/     # 参考视频
│     ├─ workflow/         # 生成的 ComfyUI 工作流 JSON
│     └─ output/           # 生成结果（回传）
├─ templates/              # 工作流 JSON 模板、brief.md 模板
└─ tools/                  # 自动化脚本（排单 / 监控 CLI）
```

### 6.4 建议我先做的三个小工具

1. **素材包 → 工作流 JSON 编译器**（Python）：读 `ep0x/assets/` + `prompts/`，按模板填 `MiniMaxH3ReferenceToVideo` 的参考槽与参数，产出可提交的 JSON。
2. **ComfyUI 排单 / 监控 CLI**（封装 API）：提交 / 查队列 / 拉结果 / WS 进度，输出"待拍板清单"。
3. **导演台文档模板** `brief.md`：标准字段（集名、人设、分镜表、镜头参数、对白）。

---

## 7. 待你补充 / 待确认 `[重要]`

- **公司内部工具 image2、大香蕉**：具体入口、能力、计费？我完全不了解，需你填（决定兜底链路怎么写）。
- **角色策略**：是否固定角色用 Ref2VA 锁定？角色图来源 / 人设文档在哪？
- **产出规格**：单集目标时长？集数？更新频率（日更 / 周更）？
- **触发方式**：WorkBuddy 主动轮询后台跑，还是你每次手动让我"排这一集"？
- **声音方案**：免费 TTS（快但机械）vs 本地 GPT-SoVITS/CosyVoice 声音克隆（拟真但需采样）？
- **首批素材优先级**：建议先积攒「角色图 + 场景图」，这是多模态参考的地基。

---

## 8. 下一步建议（你拍板我就做）

1. 搭 `ep01` 骨架目录 + `brief.md` 模板 + 一个 `MiniMaxH3ReferenceToVideo` 工作流 JSON 模板。
2. 写"排单/监控 CLI"雏形，并**实测用 API 提交一次最简 H3 任务**，打通整条自动排单链路（验证 §6.1–6.2 真实可行）。
3. 你填 §7 的待补充项后，我把素材矩阵和兜底链路补全。

---
*版本 v1.0 · 2026-08-08 · 基于台式机 ComfyUI v0.30.1 / H3 Ref2VA 节点实测*
